#include "Function.h"
#include <iostream>
#include <string>
#include<vector>
#include <stdio.h>
using namespace std;
int main (){
    const int a =5;
    int b;
    cout<<"number range :";
    cin>>b;
    int array[a];
    //sort_puziri(array,a);
     vvodfile(array,a);
}